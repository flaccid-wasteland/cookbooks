#!/usr/bin/ruby

puts "Ruby Version: "+RUBY_VERSION
puts 'Testing Storage.'

require 'rubygems'
require 'fog'

storage = Fog::Storage.new(
  :provider => 'AWS',
  :aws_access_key_id => "<% @aws_access_key_id %>",
  :aws_secret_access_key => "<% @aws_secret_access_key %>")

# list directories
p storage.directories